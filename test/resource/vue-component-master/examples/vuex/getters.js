export const tree = state => state.tree;
export const files = state => state.files;
export const editable = state => state.editable;
export const accordion = state => state.accordion;

export default {
    tree,
    files,
    editable,
    accordion
};
